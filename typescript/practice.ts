/*
VARIABLES
* Use variables to define strict types
* These variables will be restricted to the same type while coding
*/

// variable is assigned to type 'string'
let variable = "hello"
variable = "hi"

/*
This would throw an error because variable is assigned to 'string' not 'number' 
variable = 18
*/